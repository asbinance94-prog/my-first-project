# AI Radar — بريف البناء لـ Claude Code

الصقي هذا الملف كاملاً في Claude Code مع `sources.json` و `validate_feeds.py` في نفس المجلد.

---

## الهدف

لوحة أخبار شخصية تجمع أخبار الذكاء الاصطناعي من فيدات RSS ونشرات بريدية وقنوات يوتيوب، تلخّصها بالعربية، تدمج المكرر، وترتّبها حسب أولويات صانعة محتوى متخصصة في صناعة الأفلام بالذكاء الاصطناعي.

**المستخدمة الوحيدة:** أسماء — منسقة المركز الإعلامي، أكاديمية منارة، الكويت. لغة الواجهة عربية، الاتجاه RTL.

---

## القاعدة الأولى قبل أي كود

شغّلي `validate_feeds.py` أولاً. لا تُبنى أي مرحلة على مصدر لم يُفحص. المخرج `validated_sources.json` هو المدخل الوحيد لخط المعالجة.

---

## البنية

```
GitHub Actions (كل 30 دقيقة)
  └─ fetch.py      → يسحب الفيدات المفحوصة
  └─ enrich.py     → Claude API: تلخيص عربي + تصنيف + دمج المكرر
  └─ يكتب data/news.json
       ↓
GitHub Pages (موقع ثابت)
  └─ index.html    → يقرأ news.json، لا خادم، لا قاعدة بيانات
```

سبب هذا الاختيار: صفر تكلفة استضافة، يعمل والجهاز مطفأ، والتاريخ كله محفوظ في Git.

**لا تستخدم قاعدة بيانات.** ملف JSON واحد يكفي لآلاف العناصر. أصغر حل ينجح.

---

## المرحلة 1 — `fetch.py`

- اقرأ `validated_sources.json`
- اسحب كل فيد بـ `feedparser`
- خزّن حالة كل فيد في `state.json` باستخدام هيدرات

```
ETag / If-Modified-Since
```

حتى لا تُعاد قراءة نفس المحتوى وتُستهلك حصة الـ API بلا فائدة.

- بصمة التكرار: `sha256(العنوان المطبّع + الدومين)`
- احتفظ بآخر 14 يوم فقط في `news.json` — الأقدم يذهب لـ `archive/YYYY-MM.json`

### فيدات يوتيوب

القنوات في `sources.json` مذكورة بالـ handle وليس بالـ ID. اكتب `resolve_youtube.py` يحوّلها مرة واحدة:

- افتح `https://www.youtube.com/@handle`
- استخرج `channelId` من كود الصفحة
- كوّن الفيد: `https://www.youtube.com/feeds/videos.xml?channel_id=<ID>`
- احفظ الناتج في `youtube_feeds.json` — لا تكرر العملية كل تشغيل

**لا تخترع أي channel_id.** إذا فشل الاستخراج، سجّل القناة كفاشلة واعرضها في التقرير.

### النشرات البريدية

النشرات التي فيها `"method": "email"` ليس لها فيد. الحل:

1. أنشئي إيميل مخصص (مثال: `airadar@...`) واشتركي فيه بكل النشرات
2. فلتر Gmail يضع كل الوارد تحت ليبل `AI-Newsletters`
3. سكربت يقرأ الليبل عبر Gmail API ويحوّل كل رسالة إلى عنصر خبر
4. الحقل `source_type` يساوي `newsletter` لتمييزها بصرياً

هذه المرحلة **مؤجلة إلى المرحلة الثانية**. ابني RSS أولاً واشحنه.

---

## المرحلة 2 — `enrich.py`

استدعِ Claude API مرة واحدة لكل دفعة (batch)، لا مرة لكل خبر — الفرق في التكلفة كبير.

النموذج: `claude-sonnet-4-6`

### البرومبت

```
أنت محرر أخبار تقنية لمنصة عربية متخصصة في صناعة المحتوى
بالذكاء الاصطناعي. الجمهور: صانعة أفلام ومدربة معتمدة في الخليج.

لكل خبر أعد كائن JSON يحتوي:

- title_ar: عنوان عربي دقيق، أقل من 12 كلمة، بدون مبالغة ولا كلمات
  مثل "ثوري" أو "يغيّر كل شيء"
- summary_ar: جملتان. الأولى: ماذا حدث. الثانية: لماذا يهم صانع محتوى.
- category: واحدة من
  video_models | image_models | audio_voice | tools | research |
  business | policy | other
- relevance: من 0 إلى 10. المعيار: هل يغيّر هذا طريقة عمل صانع
  أفلام بالذكاء الاصطناعي خلال 30 يوم؟ خبر تمويل بلا منتج = 2.
  إصدار نموذج فيديو جديد = 9.
- content_angle: لو كان الخبر يصلح لريلز أو منشور تعليمي، اكتب
  الزاوية في سطر واحد. وإلا اتركه فارغاً.
- cluster_key: معرّف موحّد للحدث نفسه إن غطّته عدة مصادر.

قواعد صارمة:
- لا تخترع أرقاماً أو تواريخ أو أسماء نماذج غير موجودة في النص.
- إذا كان النص المُعطى ناقصاً، اكتب summary_ar بما هو موجود فقط.
- أعد JSON خالصاً بدون أي نص قبله أو بعده وبدون علامات markdown.
```

### الدمج

اجمع العناصر بنفس `cluster_key`، اعرض الخبر مرة واحدة، وأظهر عدد المصادر كشارة. **الخبر الذي غطّته 5 مصادر يُرفع تلقائياً إلى الأعلى** — تعدد التغطية إشارة صعبة التزوير.

### الترتيب النهائي

```
score = relevance × 0.5
      + source_weight × 0.2
      + عدد المصادر في العنقود × 0.2
      + حداثة (تتناقص خلال 48 ساعة) × 0.1
```

مع مضاعف ×1.5 لأي عنصر تصنيفه `video_models` أو `image_models` — هذا مجال العمل الفعلي.

---

## المرحلة 3 — الواجهة `index.html`

ملف واحد. HTML و CSS و JS معاً. بدون إطار عمل، بدون build step.

### المتطلبات

- `<html dir="rtl" lang="ar">`
- خط `Tajawal` أو `IBM Plex Sans Arabic` من Google Fonts
- المصطلحات الإنجليزية داخل النص تُلف بـ `<span dir="ltr">` وإلا انقلب ترتيبها
- يعمل بدون إنترنت بعد أول تحميل (Service Worker بسيط)
- منصّة أساسها الجوال — أسماء تتصفح من الجوال أكثر من المكتب

### التخطيط

**شريط علوي:** التاريخ، وقت آخر تحديث، عداد الأخبار الجديدة.

**قسم "الأهم الآن":** أعلى 3 عناصر بالنقاط، بطاقات كبيرة.

**تبويبات التصنيف:** الكل / نماذج الفيديو / نماذج الصورة / الصوت / الأدوات / البحث / الأعمال.

**بطاقة الخبر:**
- العنوان العربي
- ملخص سطرين
- شعار المصدر + شارة "غطّته N مصادر" إن وُجدت
- شارة `content_angle` ذهبية إن وُجدت — نقرة واحدة تنسخ الزاوية
- رابط للأصل يفتح في تبويب جديد

**زر "نسخ موجز اليوم":** ينسخ أفضل 5 أخبار كنص عربي جاهز للنشر أو لإرساله لـ د. سعود. هذا الزر هو أكثر ما ستستخدمينه فعلياً — لا تعامليه كميزة ثانوية.

### الاتجاه البصري

داكن مع ذهبي — نفس الهوية السينمائية في موقعك الشخصي. خلفية قريبة من `#0D0D0F`، نص `#E8E4DC`، لمسة ذهبية `#C9A227` للتأكيد فقط لا للزخرفة. مسافات واسعة، تباين عالٍ، بطاقات بحدود رفيعة جداً بدلاً من ظلال ثقيلة.

---

## المرحلة 4 — النشر

`.github/workflows/update.yml`

```yaml
on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:
```

- مفتاح Claude API في GitHub Secrets — **لا يوضع في الكود إطلاقاً**
- الـ workflow يعمل commit لـ `data/news.json` مع رسالة فيها الوقت
- GitHub Pages من فرع `main`
- ملف `data/health.json` يسجّل: عدد الفيدات الناجحة، الفاشلة، تكلفة آخر تشغيل

---

## المرحلة 5 (مؤجلة) — الإشعار الفوري

بعد استقرار اللوحة أسبوعين: أي خبر نقاطه فوق 8.5 يُرسل فوراً عبر Telegram Bot (أبسط وأسرع في البناء) أو WhatsApp. **لا تبني هذا الآن** — احتمال الإزعاج عالٍ قبل معايرة نظام النقاط.

---

## قواعد التنفيذ

- ابنِ بالترتيب. اشحن المرحلة 1 و 3 عاملتين قبل البدء بالتلخيص.
- لا تدّعِ أن شيئاً يعمل بدون تشغيله فعلياً وعرض المخرجات.
- كل فشل شبكة يُسجّل ولا يوقف التشغيل — فيد واحد ميت لا يعطّل اللوحة.
- سجّل تكلفة كل استدعاء API في `health.json`.
- عند أي رابط فيد غير مؤكد: **افحصه، لا تفترضه**.
